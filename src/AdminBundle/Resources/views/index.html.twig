{% extends '@Shop/main_layout.html.twig' %}
{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-xs-1">
                <a href="{{ path('admin_add_category') }}" class="btn btn-success btn-sm">Add new category</a>
            </div>
            <div class="col-xs-11">
                {% block admin_panel %}

                {% endblock %}
            </div>
        </div>
    </div>
{% endblock %}